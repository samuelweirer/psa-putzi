# API Configuration
# Use API Gateway (port 3000) - routes requests to all backend services
# Auth: /api/v1/auth/* → auth-service (port 3001)
# CRM: /api/v1/customers/* → crm-service (port 3020)
# Tickets: /api/v1/tickets/* → tickets-service (port 3030)
VITE_API_BASE_URL=http://10.255.20.15:3000

# Application
VITE_APP_NAME=PUTZI
VITE_APP_VERSION=1.0.0
